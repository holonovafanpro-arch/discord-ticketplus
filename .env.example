TOKEN=your_discord_bot_token
CLIENT_ID=your_application_client_id
# Optional: set this for instant dev command updates on one server
GUILD_ID=
# Optional default fallback role (can be changed with /config)
STAFF_ROLE_ID=
